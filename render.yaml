services:
  - type: web
    name: academic-writing-server
    runtime: node
    plan: free
    buildCommand: npm run build
    startCommand: cd apps/server && npm start
    envVars:
      - key: NODE_ENV
        value: production
